export interface IAuthState {
    // login
    loginIsLoading: boolean;
    // check email
    email: string;
    checkEmailIsLoading: boolean;
    // recovery password
    recoveryPasswordIsLoading: boolean;
    // register
    registerIsLoading: boolean;
}
