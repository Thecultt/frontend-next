import React from 'react';

import { Auth } from '@/screens';
import { AUTH_META } from '@/constants/meta';

export const metadata = AUTH_META;

const AuthPage = () => <Auth />;

export default AuthPage;
